// Create Employee class contructor with inheritable methods

// Class Constructor
class Employee {
    constructor(name = '') {
        this.name = name;
        this.id = id;
        this.email = email;
        this.role = role;
    }

    // i dont think Im setting this up right...
    getName(name) {
        let name = data.name;
    }

    getId(id){
        let id = data.id;
    }

    getEmail(email){
        let email = data.email
    }

    getRole(role){
        let role = data.role;
    }

};
